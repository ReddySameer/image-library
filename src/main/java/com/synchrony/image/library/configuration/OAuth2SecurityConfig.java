package com.synchrony.image.library.configuration;

import org.springframework.beans.factory.annotation.Value;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
//import org.springframework.security.oauth2.client.resource.OAuth2ProtectedResourceDetails;
//import org.springframework.security.oauth2.client.token.grant.client.ClientCredentialsResourceDetails;

//@Configuration
public class OAuth2SecurityConfig {
	
	/*
	 * //@Value($tokenUrl)
	 * 
	 * @Bean protected OAuth2ProtectedResourceDetails oauth2Resource() {
	 * ClientCredentialsResourceDetails clientCredentialsResourceDetails = new
	 * ClientCredentialsResourceDetails();
	 * //clientCredentialsResourceDetails.setAccess }
	 */
	
}